{
  "name": "giornale-di-bordo-ios",
  "private": true,
  "version": "1.0.0",
  "description": "Wrapper Capacitor per pubblicare Giornale di Bordo su App Store",
  "scripts": {
    "copy-web": "node scripts/copy-web.js",
    "sync": "npm run copy-web && npx cap sync ios",
    "open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.4.2",
    "@capacitor/ios": "^8.4.2",
    "@capacitor/push-notifications": "^8.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2"
  }
}
